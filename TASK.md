# TASK: Удалить venv311 из git tracking

**Создан:** 2026-04-07T18:24:45Z
**Уровень:** L1
**Статус:** pending

## Цель
Убрать директорию venv311 из git tracking (если она отслеживается) и убедиться, что она игнорируется в .gitignore.

## Scope
Файлы и директории в scope:
- [`.gitignore`](.gitignore) — проверить наличие venv311/ в ignore list
- Команды git для удаления из tracking

Вне scope (не трогать):
- все остальные файлы
- физическое удаление папки venv311 (если существует)

## Шаги
1. **Проверить текущее состояние .gitignore**
   - Убедиться, что `.gitignore` содержит `venv311/`, `venv/`, `.venv/`, `__pycache__/`, `*.pyc`
   - Если отсутствует — добавить

2. **Проверить статус venv311 в git**
   - Выполнить `git status` для проверки отслеживания
   - Если venv311 отслеживается — продолжить

3. **Удалить venv311 из git tracking**
   - Выполнить `git rm -r --cached venv311/` (если директория существует в индексе)
   - Если директория не существует в индексе — пропустить

4. **Закоммитить изменения**
   - Выполнить `git commit -m "chore: remove venv311 from repo, update .gitignore"`

5. **Запушить изменения**
   - Выполнить `git push origin main`

## Definition of Done
- [ ] `.gitignore` содержит необходимые паттерны для виртуальных окружений
- [ ] venv311 удалена из git tracking (если была отслеживаемой)
- [ ] Изменения закоммичены с осмысленным сообщением
- [ ] Изменения запушены в удалённый репозиторий

## Observations outside scope
(Engineer заполняет в конце - наблюдения вне scope, не применённые)