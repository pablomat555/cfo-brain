# TASK: Добавить handler для команды /report в bot/handlers/commands.py

**Создан:** 2026-04-05
**Уровень:** L2
**Статус:** pending

## Цель
Добавить обработчик команды /report, который вызывает API для получения отчёта за период, форматирует ответ и отправляет в Telegram.

## Scope
Файлы и директории в scope:
- `bot/handlers/commands.py` — добавление нового handler и обновление списка команд в cmd_start

Вне scope (не трогать):
- все остальные файлы (включая API endpoint, модели данных, агрегаторы)
- изменение структуры API endpoint

## Шаги
1. Проанализировать текущий endpoint `/report/period` в `api/routers/report.py` (ожидает параметры `from` и `to`)
2. Определить, как интерпретировать параметр `period=this_month`:
   - Вариант A: добавить поддержку параметра `period` в API (вне scope)
   - Вариант B: в handler вычислить даты начала и конца текущего месяца и передать как `from` и `to`
3. Реализовать handler `cmd_report`:
   - Использовать `httpx.AsyncClient` для GET запроса к `http://cfo_api:8002/report/period?from=...&to=...`
   - Обработать возможные ошибки (ConnectError, HTTP ошибки)
   - Распарсить JSON ответ (ожидается модель `PeriodReport`)
   - Отформатировать сообщение согласно спецификации:
     ```
     📊 Отчёт за [период]
     
     💰 Доходы: [total_income]
     💸 Расходы: [total_expenses]
     💵 Чистые сбережения: [net_savings]
     
     🤖 AI вердикт:
     [ai_verdict]
     ```
   - Отправить сообщение пользователю
4. Обновить `cmd_start` добавить `/report` в список доступных команд
5. Протестировать локально (если возможно)
6. Сделать git commit + git push

## Definition of Done
- [ ] Handler `/report` добавлен в `bot/handlers/commands.py`
- [ ] Handler корректно вызывает API endpoint с правильными параметрами
- [ ] Ответ форматируется согласно спецификации
- [ ] Команда `/report` упомянута в списке команд в `cmd_start`
- [ ] Изменения закоммичены и запушены в репозиторий

## Observations outside scope
- API endpoint `/report/period` не поддерживает параметр `period=this_month`. Для получения отчёта за текущий месяц необходимо вычислить даты в handler.
- Поле `ai_verdict` не возвращается в текущей реализации API (только логируется). Возможно, нужно добавить его в модель `PeriodReport` или получить отдельно. В рамках scope можно пропустить AI вердикт или использовать заглушку.